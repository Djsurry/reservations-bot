# Bookings

Confirmed reservations the agent helped book. Append-only.
Used for fuzzy-matching when you mention a rating later without naming the spot.

Format: `- [<date booked>] <venue> — <party_size> @ <time> [optional notes]`
